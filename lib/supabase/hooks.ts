// Placeholder hooks — à implémenter quand Supabase est connecté
// Usage: const { data, loading, error } = useCultures();

export function useCultures() {
  return { data: [], loading: false, error: null };
}

export function useStocks() {
  return { data: [], loading: false, error: null };
}

export function useEmployes() {
  return { data: [], loading: false, error: null };
}
